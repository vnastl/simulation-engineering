# OS / editor
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]

# Raw UK Data Service distribution — large, and licensed (do not redistribute).
# Regenerate by unzipping the archives into data/.
M_5723*.zip
/data/
/downloads/

# Large generated machine artifacts — regenerate, do not version
outputs/survey/personas.jsonl
outputs/survey/tasks/micro_keys.json

# Internal sister-project handoff note
pipeline/survey/tasks/WEIGHTED_ESTIMATES.md
